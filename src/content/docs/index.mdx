---
title: ⚡ Zenith Framework
description: Modern Python web framework with clean architecture and exceptional performance
template: splash
hero:
  tagline: Build production-ready APIs with clean architecture and exceptional developer experience
  actions:
    - text: Get Started
      link: /zenith/introduction/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/nijaru/zenith
      icon: github
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## Build Rails-like APIs in Python

<CardGrid>
	<Card title="Rails-like DX" icon="puzzle">
		Zero-config setup, ActiveRecord-style models, and one-liner features deliver 85% less boilerplate.
	</Card>
	<Card title="ZenithModel" icon="database">
		Rails-inspired database patterns: `User.where(active=True).order_by('-id').limit(10).all()`
	</Card>
	<Card title="One-liner Features" icon="rocket">
		`app.add_auth()`, `app.add_admin()`, `app.add_api()` - complex features in single commands.
	</Card>
	<Card title="Enhanced DX" icon="shield">
		Clean dependency injection with `db=DB` instead of verbose `Depends(get_database_session)`.
	</Card>
</CardGrid>

## Quick Start

```bash
# Install Zenith
pip install zenith-web

# Create a new project
zen new my-api

# Start development server
zen server --reload
```

## Why Developers Choose Zenith

<CardGrid>
	<Card title="Rails-like Productivity" icon="heart">
		Zero-config setup, ActiveRecord patterns, and automatic session management eliminate boilerplate.
	</Card>
	<Card title="Exceptional Performance" icon="rocket">
		9,600+ req/s with Rails-like features. No performance penalty for convenience features.
	</Card>
	<Card title="Production Ready" icon="shield">
		631+ tests, zero memory leaks, automatic 404s, and comprehensive middleware out of the box.
	</Card>
	<Card title="Modern Python" icon="puzzle">
		Python 3.12+ optimizations, full async/await, and complete type safety throughout.
	</Card>
</CardGrid>
