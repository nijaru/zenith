---
title: Quick Start
description: Build your first Zenith application with Rails-like DX in 5 minutes
---

import { Aside } from '@astrojs/starlight/components';

## Create Your First Rails-like API

Let's build a simple blog API to demonstrate Zenith's Rails-inspired features.

1. **Install Zenith**

   ```bash
   pip install zenith-web
   ```

2. **Create your application file**

   Create `app.py`:

   ```python
   from zenith import Zenith
   from zenith import Session  # Enhanced dependency injection
   from zenith.db import ZenithModel  # Rails-like models
   from sqlmodel import Field
   from typing import Optional

   # 🚀 Zero-config setup - just works!
   app = Zenith()

   # ⚡ Add features in one line each
   app.add_auth()    # JWT authentication + /auth/login
   app.add_admin()   # Admin dashboard at /admin
   app.add_api("Blog API", "1.0.0")  # API docs at /docs

   # 🏗️ Rails-like model with ActiveRecord patterns
   class Post(ZenithModel, table=True):
       id: Optional[int] = Field(primary_key=True)
       title: str = Field(max_length=200)
       content: str
       author: str = Field(max_length=100)
       published: bool = Field(default=False)

   @app.get("/")
   async def root():
       return {
           "message": "Welcome to Rails-like Blog API",
           "features": ["Rails-like models", "One-liner setup", "Enhanced DX"],
           "docs": "/docs",
           "admin": "/admin"
       }

   @app.get("/posts")
   async def get_posts(published: bool = True, session=Session):
       """Get blog posts with Rails-like queries."""
       if published:
           # Rails-style: Post.where(published=True).order_by('-id').limit(10)
           query = await Post.where(published=True)
           posts = await query.order_by('-id').limit(10).all()
       else:
           posts = await Post.all()
       return {"posts": [post.to_dict() for post in posts]}

   @app.post("/posts")
   async def create_post(post_data: dict, session=Session):
       """Create post with Rails-style convenience."""
       # Rails-style: Post.create() - no session management!
       post = await Post.create(**post_data)
       return {"post": post.to_dict(), "message": "Post created"}

   @app.get("/posts/{post_id}")
   async def get_post(post_id: int, db=DB):
       """Get post with automatic 404 handling."""
       # Rails-style: automatic 404 if not found
       post = await Post.find_or_404(post_id)
       return {"post": post.to_dict()}

   @app.put("/posts/{post_id}")
   async def update_post(post_id: int, updates: dict, db=DB):
       """Update post with Rails-style convenience."""
       post = await Post.find_or_404(post_id)
       # Rails-style: model.update() method
       await post.update(**updates)
       return {"post": post.to_dict(), "message": "Post updated"}

   @app.delete("/posts/{post_id}")
   async def delete_post(post_id: int, db=DB):
       """Delete post with Rails-style convenience."""
       post = await Post.find_or_404(post_id)
       # Rails-style: model.destroy() method
       await post.destroy()
       return {"message": "Post deleted successfully"}
   
   # Health check endpoint
   @app.get("/health")
   async def health_check():
       return {
           "status": "healthy",
           "timestamp": datetime.utcnow(),
           "version": app.version
       }
   ```

3. **Run the development server**

   ```bash
   python app.py
   # Or use uvicorn directly
   uvicorn app:app --reload
   ```

4. **Test your Rails-like API**

   Your API is now running at `http://localhost:8000` with these features:
   - 📚 **API Docs**: http://localhost:8000/docs (Interactive Swagger UI)
   - 🔐 **Authentication**: POST to http://localhost:8000/auth/login
   - ⚡ **Admin Dashboard**: http://localhost:8000/admin
   - 🏥 **Health Check**: http://localhost:8000/health

**Test the Rails-like CRUD:**
```bash
# Create a post (Rails-style: Post.create)
curl -X POST http://localhost:8000/posts \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Rails-like DX in Python",
    "content": "Zero config, ActiveRecord patterns, one-liner features!",
    "author": "Developer",
    "published": true
  }'

# Get published posts (Rails-style: Post.where().order_by().limit())
curl http://localhost:8000/posts?published=true

# Get specific post (Rails-style: Post.find_or_404)
curl http://localhost:8000/posts/1

# Update post (Rails-style: post.update)
curl -X PUT http://localhost:8000/posts/1 \
  -H "Content-Type: application/json" \
  -d '{"title": "Updated Title"}'
```

```

<Aside type="tip" title="Rails-like DX Benefits">
Compare this Zenith code to equivalent FastAPI + SQLAlchemy:

- **85% less boilerplate** - No manual session management, dependency setup, or middleware configuration
- **Zero-config setup** - `app = Zenith()` vs 50+ lines of FastAPI setup
- **Rails-like queries** - `Post.where().order_by().limit()` vs complex SQLAlchemy
- **One-liner features** - `app.add_auth()` vs 20+ lines of auth setup
- **Automatic 404s** - `Post.find_or_404()` vs manual error handling
</Aside>

## 🎯 What You Get Out of the Box

Your Rails-like API now includes:

### 🔐 **Authentication** (from `app.add_auth()`)
```bash
# Login with any username/password
curl -X POST http://localhost:8000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "password": "password"}'
```

### ⚡ **Admin Dashboard** (from `app.add_admin()`)
- Health monitoring at `/admin/health`
- System stats at `/admin/stats`
- Interactive dashboard at `/admin`

### 📚 **API Documentation** (from `app.add_api()`)
- Interactive docs at `/docs`
- Alternative docs at `/redoc`
- OpenAPI schema at `/openapi.json`

## Next Steps

- **[Learn ZenithModel patterns](./models.md)** - Rails-like database operations
- **[Explore examples](../examples/)** - Real-world usage patterns
- **[Add authentication](./auth.md)** - Secure your API endpoints
- **[Deploy to production](./deployment.md)** - Ready for production use


## 🚀 Ready for More?

You've just experienced the power of **Rails-like DX in Python**! In just a few lines of code, you built a production-ready API with:

✅ **Zero configuration** - No complex setup files
✅ **ActiveRecord patterns** - Rails-like database operations
✅ **One-liner features** - Authentication, admin, docs instantly
✅ **Enhanced DI** - Clean, readable dependency injection
✅ **Automatic 404s** - No manual error handling
✅ **Production-ready** - Security, monitoring, health checks built-in

### Next Steps

- **[Learn ZenithModel](../models.md)** - Master Rails-like database patterns
- **[Explore Examples](../examples/)** - See real-world usage patterns
- **[Add Authentication](../auth.md)** - Secure your endpoints with JWT
- **[Production Deployment](../deployment.md)** - Deploy with confidence

### Join the Community

- **[GitHub](https://github.com/nijaru/zenith)** - Star the project and contribute
- **[Examples Repository](https://github.com/nijaru/zenith/tree/main/examples)** - Working examples
- **[Issues](https://github.com/nijaru/zenith/issues)** - Report bugs or request features

---

**Happy coding with Rails-like DX in Python!** 🎉