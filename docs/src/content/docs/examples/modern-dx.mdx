---
title: Automatic Configuration Setup
description: Build applications without configuration files
---

import { Aside, FileTree } from '@astrojs/starlight/components';

## Overview

This example demonstrates Zenith's automatic configuration features that allow you to start building without setup files.

## Key Features

### Automatic Configuration Application

```python
from zenith import Zenith

# Automatic application configuration - detects environment and configures:
# - Database (SQLite for dev, requires DATABASE_URL for prod)
# - CORS (permissive for dev, secure for prod)
# - Security (dev key for dev, requires SECRET_KEY for prod)
# - Middleware (debug toolbar in dev, security headers in prod)
app = Zenith()
```

### Database Models

```python
from zenith.db import ZenithModel
from sqlmodel import Field

class User(ZenithModel, table=True):
    id: int | None = Field(primary_key=True)
    name: str
    email: str = Field(unique=True)
    active: bool = Field(default=True)

# Query methods
users = await User.all()
user = await User.find(1)
user = await User.find_or_404(1)  # Returns 404 if not found
user = await User.create(name="Alice", email="alice@example.com")

# Chainable queries
active_users = await User.where(active=True).order_by('-created_at').limit(10)
```

### Dependency Injection

```python
@app.get("/users")
async def get_users():
    """Get active users."""
    users = await User.where(active=True).all()
    return {"users": [u.to_dict() for u in users]}
```

## Complete Example

Run the example: `python examples/03-modern-developer-experience.py`

This example demonstrates:
- Automatic configuration
- Database model queries
- Dependency injection
- Error handling

<Aside type="tip">
  **Note**: The database session is managed automatically within request scope.
</Aside>
