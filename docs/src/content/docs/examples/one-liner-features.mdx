---
title: Built-in Features
description: Add authentication, admin panel, and API documentation
---

import { Aside } from '@astrojs/starlight/components';

## Overview

Zenith includes built-in methods for common features like authentication, admin panels, and API documentation.

## Authentication in One Line

```python
from zenith import Zenith

app = Zenith()
app.add_auth()  # Add JWT authentication

# This adds:
# - POST /auth/login endpoint
# - JWT token generation
# - User authentication middleware
# - Token refresh capability
```

## Admin Dashboard in One Line

```python
app.add_admin()  # Add admin dashboard

# This adds:
# - GET /admin - Admin dashboard
# - GET /admin/health - System health check
# - GET /admin/stats - Application statistics
# - GET /admin/logs - Recent logs viewer
```

## API Documentation in One Line

```python
app.add_api("My API", "1.0.0", "API description")

# This adds:
# - GET /docs - Interactive Swagger UI
# - GET /redoc - Alternative ReDoc UI
# - GET /openapi.json - OpenAPI schema
```

## Chain Everything Together

```python
from zenith import Zenith

# Chain methods together
app = (Zenith()
       .add_auth()
       .add_admin("/dashboard")  # Optional custom path
       .add_api("My API", "1.0.0"))
```

## Complete Example

Run the example: `python examples/04-built-in-features.py`

<Aside type="tip">
  **Note**: These methods provide sensible defaults that can be customized as needed.
</Aside>

## Manual Configuration

These built-in methods replace manual setup of JWT tokens, admin routes, and OpenAPI configuration. Each method configures multiple endpoints and middleware automatically.
